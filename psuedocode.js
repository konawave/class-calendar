// Pseudocode:
// Need the date to display at the top.
    // The hour of the time must be useable to be compared to the ID of the textAreas so that they know when to switch from future to past to present

// TextArea entries need to be saved to local storage, and added to the .textContent of the textArea when the respective button is clicked. 
    //  preventDefault here

// How to use the ID of the container of the button. 
// Hint is 'this' in click listener, also DOM traversal. 

// $('.saveBtn').on('click', function() {
//     localStorage.set('event', textBox.value)
//      for (i = 2; i === this.parentElemend.id[i])
//          textBox.append(textBox.value)
//     retreive = localStorage.get('event')
//     textBox.append(textBox.value)
//   })
// this.parentElement.id